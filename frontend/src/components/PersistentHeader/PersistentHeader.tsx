// Skeleton only — not wired into the app yet. Intended to become a
// persistent, cross-route header once navigation is implemented.
export function PersistentHeader() {
  return <header className="h-14 w-full border-b border-slate-200" />
}
