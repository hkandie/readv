# Shared configuration for jobs running on the Windows build runner

.baseconfiguration:
  tags:
    - windows-build
  retry:
    max: 2
    when:
      - runner_system_failure
      - stuck_or_timeout_failure
