spring:
  application:
    name: eadvantage-local

app:
  cors:
    allowed-origins: http://localhost:5173,http://localhost:3000

logging:
  level:
    root: INFO
    com.fhlbdm.eadvantage: DEBUG
    org.springframework: INFO
    org.springframework.security: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
